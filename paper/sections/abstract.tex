Released HR-GLDD NumPy arrays do not state their channel order. The paper's
Red--Green--Blue--NIR listing and notebook display support RGBN more strongly,
but missing array-construction records leave native PlanetScope BGRN as a
conservative sensitivity. We rerun matched controls under both mappings and
retain a contrast direction only when both agree. Index-specific F1 contrasts
remain negative, while the index-modulated boundary effect changes sign.
Generic boundary weighting remains positive in all 18 comparisons, with
mapping-level means of $+0.96$--$+1.13$ points. CAS and LRD do not confirm
transfer. A publicly locked Sen12Landslides test passes only the
NDVI-modulated-minus-plain-boundary margin, while all arm means are
$1.21$--$3.14\%$ F1. Sen12 provides no support for recurrence and does not
refute the mechanism. HR-GLDD remains descriptive because event and coordinate
identities are absent.
