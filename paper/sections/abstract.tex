Released HR-GLDD NumPy arrays lack authoritative visible-band metadata: direct notebook display supports Red--Green--Blue--NIR (RGBN), whereas native four-band PlanetScope supports Blue--Green--Red--NIR (BGRN). We apply a finite identified-set certificate: for a signed mechanism contrast, its envelope spans all metadata-consistent schemas, and direction is retained only when that envelope excludes zero. Complete RGBN/BGRN runs give index-input-versus-raw F1 envelope $[-0.21,-0.01]$ points and equal-mass index-versus-plain F1 envelope $[-0.41,-0.33]$, but the boundary-F1 envelope $[-0.05,+0.38]$ crosses zero. Generic boundary weighting remains positive, $[+0.96,+1.13]$, with all 18 individual effects positive. A post-hoc CAS analysis fails three of four criteria. An internally precommitted LRD run also fails its frozen criteria, but weak validation, trigger-family overlap, and empty-crop sensitivity make it failed/indeterminate rather than confirmation. HR-GLDD results remain descriptive because event and coordinate identities are absent.
