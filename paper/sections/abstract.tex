Released HR-GLDD NumPy arrays do not state their channel order. The paper's
Red--Green--Blue--NIR listing and notebook display support RGBN more strongly,
but missing array-construction records leave native PlanetScope BGRN as a
conservative sensitivity. For each contrast, we therefore report the range
across both mappings and retain its direction only when that range excludes
zero. Index-input-minus-raw and equal-mass-index-minus-plain F1 remain negative:
$[-0.21,-0.01]$ and $[-0.41,-0.33]$ points. The boundary-F1 range
$[-0.05,+0.38]$ crosses zero. Generic boundary weighting has a
mapping-level mean range of $[+0.96,+1.13]$ points; all 18 seed/control effects
are positive ($[+0.42,+1.62]$). CAS and LRD do not confirm transfer. A publicly
locked Sen12Landslides test passes only the NDVI-modulated-minus-plain-boundary
margin, $-0.06$ [$-0.41,+0.26$] points; all arm means are $1.21$--$3.14\%$
F1. Sen12 provides no support for recurrence and does not refute the mechanism.
HR-GLDD remains descriptive because event and coordinate identities are absent.
